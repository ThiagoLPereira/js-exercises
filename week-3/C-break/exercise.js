const names = ["Daniel", "James", "Irina", "Mozafar", "Ashleigh"];

for (var index = 0; index < names.length; index += 1) {
  var nameLength = names[index].length;
  if (nameLength < 6) {
    console.log(name[index]);
    break;
  }
}
