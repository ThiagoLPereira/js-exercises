function s(w1, w2) {
  return w1.concat(w2);
}

var result = s("code", "your");
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them.
// * The program concatenate "code" and "your".
// - How many parameters does the function take?
// * The function takes 2 parameters.
// - What is the function name?
// * The function name is S.
// - Where is the function called? with what parameters?
// * The function called in the variable result with the parameters "w1" and "w2".
// - What does the function return?
// * The function returns concatenate paramenter "w1" with "w2".
// - How can this function be improved?
// * The function can be improved chanching "w1.concat(w2)" for "w1 + w2".